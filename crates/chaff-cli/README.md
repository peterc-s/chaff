# Chaff CLI

To view the available options:

```sh
cargo run --release -- --help
```

Or install with:

```sh
cargo install --path .
```

while inside the `chaff-cli` directory. Then you can run:

```sh
chaff-cli --help
```
