# Chaff Capture

A basic trace capture library. Provides the main `Trace` `struct` used by `chaff-sim` and `chaff-datasets` and includes the Chaff trace file format. See the documentation for the current specification.
